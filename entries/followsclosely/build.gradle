apply plugin: 'java'

repositories {
    jcenter()
}

dependencies {
    compile(project(':core'))
    compile(project(':swing'))

    compile(project(':entries:ryanp'))

    testCompile 'junit:junit:4.12'
}